import React, { Component } from 'react';

class Header extends Component {
    render(){
        return(
            <h1 className='header_text'>Search for books</h1>
        );
    }
}

export default Header;